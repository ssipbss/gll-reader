# 归零归零（跟打朗读软件）交接文档

> 给新聊天窗口的说明：请先读本文件、`计划.md`、`使用说明.txt` 和 `src/` 代码，再继续维护。

## 项目位置

`E:\Lin\codex\跟打朗读软件`

> 桌面上的“归零”文件夹是入口（含启动软件、打开项目、打开安装包的快捷方式），项目本体在 E 盘。

- 成品：`发布\归零归零.exe`（用户直接运行这个）
- 源码：`src\`（C#，WinForms，.NET Framework 4.8，用系统自带 csc.exe 编译，无需安装 SDK）
- git 仓库：本目录已初始化，历史版本都在（`git log` 可查）

## 软件功能

一边打字一边朗读的桌面小工具：

1. 全局键盘监听（低级钩子），所有软件里打字都有效；
2. 字母 A-Z 朗读（英文语音，强制逐字母，用 SSML say-as）；
3. 数字朗读（一、二、三……单个数字）；
4. 中文朗读：拼音/五笔组字上屏后朗读（通过“读输入框文字变化”实现，不依赖输入法接口）；
5. 功能键朗读：回车、退格、删除、空格、F1-F12 等（方向键静音）；
6. 中文输入法状态检测：键盘布局 0x0804 系 + “最近读中文”双保险；Shift 切换英文模式后字母朗读；
7. 启动即缩托盘，关闭按钮缩回托盘（真正退出用托盘菜单“退出”）；
8. 设置存 `%APPDATA%\GuiLingGuiLing\settings.xml`（不随 exe 移动丢失）。

## 关键技术实现

- 键盘钩子：`src/KeyboardHook.cs`（WH_KEYBOARD_LL，带自动重复识别）；
- 字符翻译：`src/KeyTranslator.cs`（ToUnicodeEx + 键盘状态）；
- 语音：`src/Speaker.cs`——**专用后台线程 + 同步队列**；合成到临时 WAV（快），**自动裁剪引擎静音尾巴**，再用 SoundPlayer 播放（单字间隔 0.5~0.7 秒）；
- 语音列表：SAPI 经典语音 + OneCore 语音（康康/瑶瑶/慧慧，通过 SpObjectTokenCategory 枚举）；
- 中文上屏识别：`src/TextReader.cs`（UIA 读焦点控件文字）+ `src/ImeMonitor.cs`（输入法布局）+ 主窗体里的 diff/去重逻辑；
- 界面：`src/ModernControls.cs`（自绘控件）+ `src/MainForm.cs`；
- 图标：`tools/IconMaker.cs` 生成（20×20 像素字体 ZERO），用 夕体Pro.ttf 不需要（当前是硬编码像素字）。

## 构建与测试

```powershell
# 构建（自动编译主程序到 发布\归零归零.exe）
powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1

# 沙箱自测（模拟按键，检查朗读决策）
powershell -NoProfile -ExecutionPolicy Bypass -File .\测试\run_test.ps1
```

注意：
- csc 位于 `C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe`；
- 构建脚本用“响应文件”方式传参（避免中文路径编码问题）；
- 修改 `src/` 后必须重新 build，发布目录的 exe 才会更新；
- 软件运行时 exe 会被占用，更新前先结束进程。

## 调试

- 主界面勾选“记录调试日志”后，`发布\debug.log` 会记录每次按键、输入法状态、朗读内容；
- 日志里 `KEY/IME/CHARS` 是输入侧，`ZH:/EN:` 是入队，`ZH_MERGE/EN_MERGE` 是实际发声；
- 语音线程另有诊断钩子（`Diag`，当前为空实现）。

## 用户偏好（重要）

1. 中文输入法组字时**字母必须静音**，只读上屏的中文；
2. 汉字**一字一读**，不要词组合并（用户明确否掉词组方案）；
3. 数字即打即读、单字读音（不要“十二”这种组合读法）；
4. 英文只读字母（W-E），绝不读成单词；
5. 中文输入法下按 Shift 切英文后要能朗读字母；
6. 设置必须持久保存（用户非常在意）；
7. 界面极简白色风；图标是 20×20 像素“ZERO”；
8. 用户当前语速 2（3 觉得尖）；英文语音内部 +2 档。

## 已知问题 / 近期修复

- 语音引擎在每字后加约 750ms 静音 → 已自动裁剪（v2.0）；
- 微软拼音音节分隔符“'”曾被误读为“单引号” → 已修复（UIA 朗读过滤掉 ASCII 引号）；
- 设置曾因初始化顺序被默认值覆盖 → 已加 `_loading` 锁；
- Shift 键码 0xA0 曾导致英文切换失效 → 已兼容；
- 某些软件以管理员运行时会读不到按键（Windows 安全限制，需以管理员运行本软件）；
- Codex 等网页输入框发送后曾误读 → 已加“焦点元素变化即静默重置”。
- 数字/字母中途突然全部静音（键盘钩子被 Windows 静默摘除，主界面仍以为在监听）→ 已修复（v2.1）：键盘钩子和鼠标钩子移到独立后台线程专职收事件，主界面只负责决策，UI 卡顿不再导致钩子掉线；注意：此修复需真机验证，沙箱测不到真实按键路径

## 待办/可能继续的需求

- 用户说“还有些小细节可能要改”，具体未说，等新聊天里告知；
- 可考虑：开机自启、更自然的语音（WinRT 晓晓/云希未安装）、托盘菜单完善。

## git 历史

`git log --oneline` 可看全部版本（v0.9 到 v2.0），随时可回滚。

## 开发过程与踩坑记录（按“现象 → 根因 → 最终方案”）

### 语音部分

1. **完全没有声音（早期）**
   - 现象：软件启动后静音。
   - 根因：SpVoice 的 RunningState 状态查询在这台机器上不可靠（读完还显示“正在朗读”），排队逻辑一直等“空闲”永远等不到。
   - 方案：弃用状态轮询，改用**独立后台线程 + 同步 Speak 队列**（`Speaker.cs`），每句完整读完再读下一句，不依赖状态查询。

2. **切换语音后卡死约 10 秒（康康男声）**
   - 现象：选了康康后全部静音，日志显示 SetVoices 卡 10 秒。
   - 根因：在“已被枚举过经典语音的 SpVoice 对象”上再切换 OneCore 语音（`voice.Voice = token`）会卡。
   - 方案：切换语音时**重建全新的 SpVoice 对象**，且**OneCore 语音优先选择**（`ApplyVoices`）。

3. **快速打字丢字（“我好吗”读成“我好”）**
   - 现象：连续上屏的字被吞。
   - 根因：旧方案“新字打断上一句”（purge），快打时上一字没读完就被掐断。
   - 方案：改成**排队朗读，不打断不丢字**；连续输入自然合并（v1.0~v1.3 曾用攒字窗，用户否掉后已移除，保持即打即读）。

4. **单字间隔太大（1.1~1.3 秒）**
   - 现象：一字一打时每字间隔很长，用户不满意。
   - 排查：先误以为引擎每句启动开销大；实测发现**每字音频本身只有约 150ms，其余约 750ms 是引擎加的静音尾巴**。
   - 方案：**合成到临时 WAV + 自动裁剪首尾静音 + SoundPlayer 播放**（`TrimWavSilence`），单字间隔降到 0.5~0.7 秒。

5. **英文被读成单词（WE 读成 “we”）**
   - 根因：SAPI 会把大写字母串当成单词。
   - 方案：用 SSML `<say-as interpret-as="characters">` 强制逐字母，且英文语音内部 +2 档语速。

### 中文输入法 / 上屏识别

6. **Win11 新式输入法中文上屏读不出来**
   - 现象：拼音/五笔上屏后无朗读。
   - 根因：微软五笔/拼音（TSF）下 Imm32 跨进程读取返回 0（imc=0），传统 IME 接口全部失效——这也是老“读霸”失效的原因。
   - 方案：改为 **UIA 读焦点输入框文字 + 前后 diff**，只朗读新增的汉字/标点（`TextReader.cs` + `CheckUiText`）。

7. **五笔“告诉Q我”被读成“告诉 Q 我”**
   - 排查过程：先以为是布局识别问题（微软五笔 HKL=00000804 未被识别，已修复识别）；后从日志发现真相——**上屏检测把组字残留字母一起读了**（`UI_DIFF [告诉q]`，q 是五笔码残留）。
   - 方案：朗读前过滤上屏内容里的英文/数字，只读汉字和标点（`FilterForSpeech`）。

8. **拼音分隔符单引号被误读（没打引号却读“单引号”）**
   - 根因：微软拼音自动在音节间插入 `'`（如 `x'la'b'...`），上屏后被当标点朗读。
   - 方案：UIA 朗读过滤 ASCII 单双引号（英文模式手打引号仍走按键路径正常读）。

9. **中文输入法组字时字母被朗读**
   - 根因：中文模式判断不稳定（候选窗口抢前台、HKL 识别遗漏）。
   - 方案：多重保险——HKL 0x0804 全系识别 + 1.5 秒滞回 + “最近读过中文”10 秒窗口 + `_composing` 组字标志；中文模式字母一律静音（Shift 切换英文后除外）。

10. **Shift 切换英文后字母仍不朗读**
    - 根因：真实键盘左 Shift 的键码是 **0xA0**，代码只认 0x10。
    - 方案：兼容 0x10/0xA0/0xA1（修饰键判断同步修正）。
    - 后续坑：英文模式会被文本里残留的中文误“校准”回中文 → 只有真正的汉字上屏才校准（`MarkChineseCommitIfCjk`）。

11. **Codex 等网页输入框发送后误读（“斜杠斜杠”）**
    - 根因：发送后焦点从输入框跳到聊天记录，文字变化被当成新输入。
    - 方案：**焦点元素变化时静默重置基线**（`_lastUiElement`）。

12. **鼠标点击文字被朗读**
    - 根因：点击导致焦点/文字变化被误判。
    - 方案：全局鼠标钩子记录点击时间；“点击文字时朗读”默认关闭，点击引起的首次文字变化直接忽略。

### 设置 / 界面 / 其它

13. **设置每次打开恢复默认**
    - 根因：界面初始化时勾选框/滑杆的默认值触发了“保存”，把设置文件覆盖成默认值（初始化锁 `_loading` 未在 BuildUi 前生效）。
    - 方案：构造一开始就 `_loading = true`；设置改存 `%APPDATA%\GuiLingGuiLing\settings.xml`（旧文件自动迁移）。

14. **长按退格刷屏“退格退格退格”**
    - 方案：键盘钩子里用按键状态数组识别**自动重复**，重复键不再朗读。

15. **图标反复返工（最终定稿）**
    - 试过：渐变圆环“00”、矢量“零”、AI 风格、像素“零”（加粗糊成一坨、O 不完整）。
    - 最终：**20×20 硬编码像素字体 “ZERO”**（4×7 字形手写，O 圆环完整，白色直角底，占中间区域），生成器 `tools/IconMaker.cs`。

16. **测试声音惊吓过用户一次**
    - 根因：测速程序直接向声卡朗读（“现、现在、字字字”）。
    - 约定：**以后所有测试只写语音文件、不发声**。

### 开发环境 / 工程坑（给后续维护者）

17. **csc 中文路径/编码问题**
    - PowerShell 5.1 读无 BOM 的 UTF-8 脚本会乱码 → 脚本用 UTF-8 BOM；
    - csc 命令行传中文路径会解析失败 → 用**响应文件（.rsp）+ 相对路径**编译；
    - `src/*.cs` 用 `/codepage:65001` 编译。

18. **apply_patch 对已存在文件经常失败**
    - 现象：Update/Delete 报 sandbox helper 错误。
    - 应对：改用 PowerShell 行号/字符串替换；**务必先 ReadAllLines 看准行号再改**（本会话多次因行号错位引入语法错误）。

19. **沙箱与真实桌面差异**
    - 沙箱内无法发声、无法模拟真实按键/输入法 → 需要真机验证的功能（中文上屏、语音、Shift 切换）要跑到用户桌面上测；
    - 沙箱对 `%APPDATA%` 读取受限 → 涉及用户设置的操作要留意权限。

20. **git 环境**
    - 仓库在 `E:\Lin\codex\跟打朗读软件`，提交需在真实用户权限下执行（沙箱对 .git 只读）；
    - 约定：每个功能版本一个提交，`git log --oneline` 可回滚。
