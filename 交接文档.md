# 归零归零（跟打朗读软件）交接文档

> 更新日期：2026-08-08
> 给新接手者：请先完整读本文件，再读 `src/` 代码和 `使用说明.txt`，最后看 git 历史，再开始动代码。

## 0. 一句话说明

Windows 桌面“跟打朗读”工具：打字时朗读刚上屏的新内容，不读旧字、不读未上屏的编码/候选，
尽量低延迟、不拖累电脑性能、不闪退。目前以多多五笔 + 微软五笔为主要使用场景。

## 1. 项目位置与基本操作

- 项目本体：`E:\Lin\codex\跟打朗读软件`
- 成品：`发布\归零归零.exe`（用户直接运行；启动后缩托盘，托盘菜单可退出）
- 源码：`src\`（C# WinForms，.NET Framework 4.8，系统自带 csc 编译，无需 SDK）
- 构建：在项目根目录运行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1`
  - 构建用响应文件 `.rsp` + 相对路径（中文路径传参会失败）
  - 软件运行时 exe 被占用，更新前先结束进程
- 日志：`发布\debug.log`（主界面勾选“记录调试日志”后才会写；用 UTF-8 查看）
- 设置：`%APPDATA%\GuiLingGuiLing\settings.xml`
- 安装/语音：`安装包\`、`install.ps1`、`一键安装.bat`；语音适配器装在
  `C:\Program Files\NaturalVoiceSAPIAdapter`（晓晓/云希来自讲述人安装版，非 HD 版）

## 2. 用户需求（最重要，按原话/决策整理）

### 2.1 朗读核心（红线）

1. **朗读准确 + 低延迟**（原话：“朗读准确和低延迟是我的需求”）。
2. 只读刚打出的新内容，**不重复读旧字**；不打补丁式修复，要查根因。
3. 中文输入法打码（五笔编码）时**字母必须静音**，只读上屏的中文。
4. Shift / 大写锁定切英文后，字母**直接上屏、立即朗读**；**鼠标手动切换**英文也要能正确朗读。
5. 中文状态下可以卡文思考：**4 个码以内不上屏不读**；超过 4 个字母仍不上屏才判断为英文。
6. 一字一读（用户明确否掉词组合并）；数字单个读（一、二、三）；英文**逐字母**读，绝不念成单词。
7. 粘贴（Ctrl+V/Shift+Insert）的内容不朗读；超过 20 字的大段上屏直接静默。
8. 未上屏的字母/候选**一律不读**；不要识别候选窗（用户明确不要）。

### 2.2 功能键与音效

- 空格 = `Windows Critical Stop.wav`，退格 = `Windows Ding.wav`，回车 = 英文单词 Enter。
- 大写锁定等功能键念英文单词（如 Caps Lock），不念“大写锁定”四个字。
- 修饰键有开关；Shift+标点只念标点（如 Shift+“ 念“引号”，不念“Shift 引号”）。
- **音效不插队**：按触发顺序排队，谁先打先念谁（曾出现空格音效覆盖刚打出的“夺”字）。
- 字母单字要听得清（E/I/O/A 曾太短、像被截掉首尾），速度不要调太快。

### 2.3 选中朗读

- 选中文本后在**选中高亮末尾**出现悬浮“开始朗读”按钮；不选中不出现。
- 点击后按钮变“结束朗读”；朗读结束按钮自动消失；再点结束则停止并消失。
- 按钮**不跟随鼠标跑**；取消选中后按钮消失，**晃动鼠标也不能再冒出来**。
- 朗读过程**不能剪切/替换选中文字**（Word 曾出现剪切问题）。
- WPS、Office、作家助手、Codex、Edge 等都要能用；多轮测试（至少两轮）都稳定才算过。

### 2.4 性能与稳定（用户非常在意）

- 不能拖累电脑：WPS 新建文档不卡、鼠标不卡、打开其它软件不卡。
- 不能闪退：WPS / Codex / Edge / 归零本程序都出现过崩溃或无响应，属严重问题。
- 托盘状态识别不要每毫秒截图（内存/CPU 占用要小）；状态切换要尽量即时感知。

### 2.5 输入法与托盘状态

- 常用输入法：**多多五笔**（主用，IMM 输入法）+ **微软五笔**（TSF）；**不换输入法**。
- 托盘图标识别是状态权威：
  - 中文 = icon_5（“中”字）或 icon_23（橙红禁圈，有时出现）
  - 英文 = icon_2 / icon_22（键盘形/“英”）
  - 深色、浅色主题都要能识别（历史上深色主题出过问题）。
- 多多五笔托盘按钮名称固定为“中文/英文”，**不随状态变化，不得采信**；
  微软五笔名称是“中文模式/英语模式”，动态可采信。

### 2.6 界面选项

- “点击文字时朗读”改为“**选中文字时朗读**”；去掉“选中文字时显示朗读按钮”选项。
- 修饰键说明不要带括号内容（界面显示不全）。
- 关闭字母朗读后字母不再发声；标点取消勾选后不再读；功能键（除 Shift 外）有开关。

### 2.7 语音

- 使用晓晓（Xiaoxiao）、云希（Yunxi），讲述人安装版（非 HD 版）。
- 功能键念英文单词；英文语音内部 +2 档语速（历史设定）；当前用户语速约 2。
- 换语音不能卡死；快速打字不丢字、不打断（排队朗读）。

### 2.8 工程偏好（用户反复强调）

- **找根因，拒绝“修修补补”**；一次只改一件事，真机验证后再改下一件。
- 测试只写语音文件/日志，**不要在用户耳边突然发声**。
- 改完自己多轮自测，确认没问题再叫用户测试。
- 每个可用版本先提交 git；大改动前保留可回退点（标签）。

## 3. 当前架构（截至 2026-08-08）

### 3.1 状态判断（关键）

- `TrayOnlyStateMode = true`（`src/MainForm.cs` 常量）：托盘识别为**唯一权威**。
  - `_trayImeEnglish` 只由两条来源写入：
    1. 图标通道 `TrayImeIconTracker`（认图，1 秒一帧，连续 N 帧一致才确认，见待办 1）；
    2. Shift 轻按翻转（`HandleKeyUp` 的 `SHIFT_TAP_FLIP`）。
  - 名称通道 `ApplyTrayImeState` 只对微软系动态名称生效；固定“中文/英文”已被忽略。
- 旧规则（程序记忆 + Shift 翻转、英文直通、字母缓冲、IMM 中文布局判断）在
  `TrayOnlyStateMode` 下**暂不生效**，代码还在，试用稳定后删除。
- 图标模板：`src/TrayImeTemplates.cs`（icon_5/icon_23 = 中文，icon_2/icon_22 = 英文；
  模板是从多多五笔本体 `C:\Windows\System32\086.ime` 重新提取的干净版本）。

### 3.2 输入与朗读通道

- 键盘钩子 `KeyboardHook.cs`（低级钩子，独立线程收事件，主界面只决策）。
- 中文上屏：文档差异通道（UIA 读焦点输入框 + diff），`CheckUiText` /
  `TrySpeakInserted` / `ComputeInsertedSmart`（异步查询，不阻塞界面线程）。
- TSF 注入：**已停用**（历史闪退根源；新版 Codex 打包应用也拒绝注入，err=126）。
- 语音 `Speaker.cs`：后台线程 + 同步队列，合成临时 WAV、裁剪静音尾巴、SoundPlayer 播放。
- 选中朗读 `SelectionSpeakController.cs`：UIA 选区监听 + 悬浮按钮 + 复制选区朗读。
- WPS/ET/WPP 在“慢应用”名单（`TextReader.IsKnownSlowApp`），**不轮询 UIA**（轮询会卡死 WPS）。

### 3.3 文件清单

- `src/MainForm.cs`（主窗体 + 按键决策 + 语音队列 + 状态机）
- `src/MainFormTrayIme.cs`、`src/MainFormUiText.cs`（partial：托盘识别 / 文档差异通道）
- `src/SelectionSpeakController.cs`（选中朗读控制器，`ISelectionHost` 接口由 MainForm 实现）
- `src/TrayImeIconTracker.cs`、`src/TrayImeTemplates.cs`（托盘图标识别）
- `src/KeyboardHook.cs`、`src/MouseHook.cs`、`src/KeyTranslator.cs`、`src/Native.cs`
- `src/Speaker.cs`、`src/SpeechBatch.cs`、`src/SpeechText.cs`、`src/RecentSpeech.cs`
- `src/TextReader.cs`、`src/ImeMonitor.cs`、`src/AppStateTracker.cs`、`src/EnPassTracker.cs`
- `src/TsfBridge.cs`（TSF 相关，已停用但保留）
- `tools/`：探针/诊断工具；`测试/`：测试脚本与输出；`test_bin/`：编译的探针 exe

## 4. 当前版本状态（重要）

- **基线 HEAD = `2a27224`**（A 版 + 干净图标模板）：用户评价“比最新版好用、不卡”。
- 标签 **`current-v-20260806`** = 旧“最新版”：用户认为几乎不能用（卡顿、问题多），
  已按要求回滚；git 内可随时对比/切回。
- **工作区有未提交改动**：另一 AI 做的“结构重构”（拆 MainForm、新建
  `MainFormTrayIme.cs` / `MainFormUiText.cs` / `SelectionSpeakController.cs`、
  图标确认 2 帧改 3 帧、新增“翻转后 8 秒拒绝相反确认”）。
  - 审查结论：**结构拆分可保留**（编译通过、接线完整、选区查询改异步是真优化）；
  - 但两处状态机改动有风险，**建议先调整再作为新基线**（见待办 1）。
- 之前的已确认修复都在工作区里：固定名称“中文/英文”不采信、图标识别详细日志
  （`TRAY_ICON_RECT` / `TRAY_ICON_TICK`）。

## 5. 开发过程中遇到的问题（现象 → 根因 → 方案/状态）

### 5.1 语音与朗读引擎

1. 完全没有声音：SpVoice 状态轮询不可靠 → 改独立后台线程 + 同步队列（Speaker.cs）。
2. 切换语音卡死约 10 秒：在已枚举语音的 SpVoice 上切换会卡 → 切换时重建 SpVoice，
   OneCore 语音优先。
3. 快速打字丢字（“我好吗”读成“我好”）：旧方案新字打断旧字 → 排队朗读不打断。
4. 单字间隔 1.1~1.3 秒：引擎每字加约 750ms 静音尾巴 → 合成 WAV + 裁剪首尾静音，降到 0.5~0.7 秒。
5. 英文被读成单词（WE→we）：SAPI 把大写串当单词 → SSML `say-as="characters"` 强制逐字母。

### 5.2 中文上屏与输入法

6. Win11 TSF 输入法（微软五笔/拼音）上屏读不出：Imm32 跨进程读取失效 →
   改 UIA 读焦点输入框 + 前后 diff（TextReader.cs + CheckUiText）。
7. “告诉Q我”被读成“告诉 Q 我”：diff 把组字残留字母一起读了 → 朗读前过滤英文/数字
   （FilterForSpeech）。
8. 拼音分隔符单引号被误读：UIA 朗读过滤 ASCII 引号。
9. 组字时字母被朗读：多重保险（HKL 0x0804 全系 + 滞回 + 最近读中文 + `_composing`）。
10. Shift 切英文后字母不读：左 Shift 键码是 0xA0 → 兼容 0x10/0xA0/0xA1。
11. Codex 发送后误读旧内容：焦点元素变化时静默重置基线。
12. 鼠标点击文字被朗读：全局鼠标钩子记录点击，点击引起的首次变化忽略。

### 5.3 TSF 与崩溃（严重）

13. WPS / Codex / Edge 反复闪退、归零无响应：TSF 注入（消息钩子里创建 TSF 管理器 +
    sink）重入风险极高 → **TSF 整体停用**，中文朗读改文档差异 + 托盘 + 按键通道。
14. 新版 Codex（打包应用）拒绝 DLL 注入（err=126）：Codex 中文朗读只能靠
    文档差异 + 托盘状态，无 TSF；需持续真机验证。
15. 键盘钩子会**静默失效**（installed=True 但收不到按键）：历史多次复现；
    “Raw Input + 钩子”双源方案在 A 版未接入（见待办 4）。
16. 界面线程被 UIA 拖死：选区监听刷屏订阅、UI 定时器丢 tick、CheckIme 50ms 轮询、
    选区查询同步 Wait(400ms) → 全部异步化；当前工作区已把选区查询也改异步。

### 5.4 托盘状态识别

17. 多多五笔托盘名称固定“中文/英文”，每秒把正确状态改回中文 → **固定名称不采信**
    （ParseTrayImeName 对“中文/英文”“英文/中文”返回 null）。
18. 图标模板被污染（icon_2/icon_22 有马赛克挡住“英”）→ 从 086.ime 重新提取干净模板。
19. 深色主题下图标识别曾失败：模板修复后需真机确认（深色/浅色都要测）。
20. icon_23（橙红禁圈）也是中文状态，识别时先查橙色像素。
21. 手动切换英文后识别延迟：图标 1 秒一帧 + 连续确认；当前工作区改 3 帧（约 3 秒）
    **与低延迟需求相反**，建议改回 2 帧（见待办 1）。
22. “翻转后 8 秒拒绝相反确认”保护：会冻结错误状态最长 8 秒（手动切换后状态未跟上、
    Shift 翻错方向时正是这种场景）——不建议对图标通道使用（见待办 1）。

### 5.5 WPS / 性能

23. WPS 新建文档卡死/转圈、打开就卡：WPS 是 Qt UIA，轮询则卡、不轮询则不读 →
    A 版把 WPS 放“慢应用”名单不轮询（不卡了，但 WPS 中文朗读仍依赖按键/IMM 通道）。
24. 鼠标卡顿：UIA 选区查询同步等待 + 频繁订阅 → 异步化 + 事件门控（当前工作区已改）。
25. 托盘截图轮询内存/CPU：有优化空间（可改事件/降频，见待办 9）。

### 5.6 选中朗读

26. 按钮位置随机/跟着鼠标跑 → 锚定到选中高亮末尾；仍有“取消选中后晃鼠标按钮重现”问题。
27. Word 朗读时剪切选中文字 → 复制流程改 WM_COPY / Ctrl+C（只复制不剪切）+ 收起选区；
    曾多次反复，真机回归。
28. WPS 里按钮不出现 → 拖动手势兜底（drag-fallback）；用户反馈 WPS 仍时好时坏。
29. 多轮测试第二三轮无声/点开始没反应 → 控制器状态机问题，需多轮回归。
30. 朗读中鼠标点其它地方，结束按钮消失导致无法结束 → 朗读期间按钮保持“结束朗读”。

### 5.7 其它

31. 重复朗读（打“万万”读“曾经”、Codex 打“曾经”念两次、作家助手重复）：
    多通道互踩 + 去重不彻底，已多次修复；机制复杂，任何新改动都要回归。
32. 未上屏字母被朗读：多通道压制；**字母缓冲方案被用户否决**（卡文思考时会读出来）。
33. 空格提示音覆盖刚打出的字（“夺”被吞）→ 音效排队不插队（已修）。
34. 在线跟打网站出现“词组充满”（未打的词被读）：候选窗/网页内容被误读，**根因未定**
    （见待办 6）。
35. 远程（手机远程桌面）输入：能朗读但时有时无，用户在意的是**不确定性**而不是远程本身。
36. 微软五笔长按 Shift 输入英文：曾念“Shift”、字母漏读——已修，需回归。
37. 修改多多五笔二进制（让托盘名称分开显示“中文/英文”）→ **不可行**：
    “中文/英文”写死在程序数据区（086.ime 的 .rdata），改显示需逆向改程序逻辑，
    风险高且更新即失效；不做。
38. 小狼毫（Rime）试用后用户不满意 → 已卸载清理。

## 6. 待解决问题（按优先级）

1. **收尾当前未提交的结构重构**（做之前先 `git stash` 或打标签备份）：
   - 图标确认从 3 帧改回 2 帧；
   - 去掉图标通道的“8 秒拒绝相反确认”（图标是权威）；名称通道的同类保护缩短到
     ≤2 秒（只防微软五笔异步名称查询返回旧值）；
   - 编译通过后，按 §8 真机回归，确认无回归再提交为新基线。
2. **手动切换英文的朗读回归**：鼠标切英文 → 打字 → 切中文 → 再切英文，观察
   `TRAY_ICON_TICK` / `SHIFT_TAP_FLIP` / `TRAY_ICON_STATE` 日志，定位是识别延迟
   还是翻转方向错误。
3. **选中朗读遗留问题**（用户说“后面再处理”）：WPS 按钮、Office 剪切、
   按钮位置/幽灵按钮、多轮稳定性。
4. **键盘钩子静默失效**：接入 Raw Input 双源 + 自愈，真机验证长时间使用不掉线。
5. **Codex 中文朗读**（无 TSF 注入）：依赖文档差异 + 托盘状态，持续验证。
6. **候选窗/网页误读“词组充满”**：查根因，禁止读候选窗与无打字痕迹的内容。
7. **界面旧规则清理**：`TrayOnlyStateMode` 稳定后删除“程序记忆+Shift翻转、英文直通、
   字母缓冲、IMM 中文布局判断”相关死代码。
8. **打包发布**：安装包与语音（晓晓/云希 + NaturalVoiceSAPIAdapter）随包，
   使用说明/发布说明同步；换电脑一键安装流程（install.ps1）。
9. **性能**：托盘截图轮询降频/事件化；Raw Input 双源接入后的开销监控。
10. **远程输入**：明确行为或加开关，避免“时读时不读”的不确定性。
11. 开机自启（用户提过“可考虑”，未拍板）。

## 7. 参考设计与资料位置

- 结构重构设计文档（在 `current-v-20260806` 分支/标签内，A 版回滚后工作区没有）：
  `docs/superpowers/specs/2026-08-06-structural-rework-design.md`
- 多多五笔安装包：`E:\Lin\下载\浏览器下载\多多五笔-龙版（86版）3.2_X64.exe` 等；
  本体 `C:\Windows\System32\086.ime`（v3.2.0.158）；干净图标 `C:\tmp\dd_icons_fresh\`
- 语音包：`C:\tmp\Xiaoxiao_1.0.9.0.msix`、`C:\tmp\Yunxi_1.0.4.0.msix`；
  适配器安装于 `C:\Program Files\NaturalVoiceSAPIAdapter`
- 历史方案文档：`思路记录-按程序跟踪输入状态.md`、`选中朗读-方案v2.md`、`计划.md`

## 8. 真机回归测试清单（每次改动后）

软件：Codex（聊天输入框）、Edge、WPS、记事本、作家助手、B 站客户端
输入法：多多五笔、微软五笔（各测一遍）
状态切换：鼠标手动切中/英、Shift 轻按、大写锁定
场景：
- 中文打码上屏（每个字都念、不念未上屏字母）
- 英文直输（逐字母念；快速打字不吞字、不重复）
- 中文卡文思考（4 码内不读）
- 粘贴大段文字（不读；>20 字静默）
- 空格/退格提示音、回车念 Enter、音效排队不插队
- 选中朗读：至少两轮（出现按钮 → 开始 → 结束 → 自动消失 → 再选再读）
- 主题：深色 + 浅色各测一遍托盘识别
- 稳定性：连续使用 30 分钟以上，观察 WPS/Codex/Edge/归零 是否卡顿、闪退、静默

日志验证要点（`发布\debug.log`）：
- `TRAY_ICON_TICK v=...` / `TRAY_ICON_STATE`：托盘识别每一帧与最终状态
- `SHIFT_TAP_FLIP` / `SHIFT_TAP_FLIP_SKIP`：Shift 翻转是否正确
- `TRAY_IME_STATE_SKIP recent-flip`：8 秒保护是否误拦（若保留）
- `TSF_COMMIT` / `UI_DIFF` / `ZH:` / `EN:`：上屏与朗读内容
- `KEY_SOUND_DEFER` / `PLAY_START` / `ZH_PLAY_END`：音效排队

## 9. 给接手者的话（替用户考虑的事项）

用户没有明确提、但必须有人跟进的：

1. **发布与迁移**：用户可能要换电脑。语音（晓晓/云希）和
   NaturalVoiceSAPIAdapter 需要安装说明或一键脚本；设置文件位置要写清楚；
   安装包目录要随版本同步。
2. **备份**：git 里保留可用版本标签；每次大改动前打标签；未提交改动要及时提交，
   避免“旧版好用新版崩了却找不到旧版”的情况。
3. **不要改系统输入法**：多多五笔是闭源程序，二进制修改不可行也不建议；
   任何“改输入法”的思路先否决，改从软件侧读图标/状态。
4. **性能是功能**：用户对卡顿的容忍度很低；任何轮询/截图/同步等待都要评估开销。
5. **日志纪律**：疑难问题先加日志/探针复现，再改代码；不要凭感觉猜。
6. **测试纪律**：测试不要发声；多轮真机回归后再叫用户；一次只改一件事。
7. **远程/权限**：Codex 会话的权限模式会影响能否写 E 盘项目；用户设置“完全访问”
   后不要再手动请求授权。远程桌面输入属于边缘场景，先确认再优化。
8. **语音版权/体积**：晓晓/云希语音包体积大（几十 MB），发布时注意随包策略。
9. **Codex 特殊**：新版 Codex 是打包应用，无法注入 DLL，中文朗读只能靠
   文档差异 + 托盘，这是物理上限，别再用 TSF 注入去试。
10. **性能监控**：托盘截图轮询是后台常驻开销，用户反馈过内存占用；后续可事件化。

## 10. git 参考

```powershell
# 注意：E 盘仓库需先声明 safe.directory
git -c safe.directory=E:/Lin/codex/跟打朗读软件 status

# 当前基线
git log --oneline -5

# 回退到“用户觉得好用”的 A 版 + 图标修复
git reset --hard 2a27224

# 旧“最新版”（用户觉得卡）作对比
git checkout current-v-20260806
```

约定：每个功能版本一个提交；发布前先提交；大改动前打标签。
