# 思路记录：按程序跟踪输入法状态（2026-08-02）

## 背景问题

归零归零目前靠"Shift 3 秒猜测"判断中/英文模式，猜测错误时会把中文组字字母误读成英文。
根因：Chromium 类软件中读不到输入法（多多五笔/微软五笔）内部的中英开关（imc=False）。

## 用户提出的方案（已验证规则成立）

实测确认（多多五笔）：
- 单次轻按 Shift = 切换一次中/英文（第 1 次按 → 英文；第 2 次按 → 中文）
- 每个程序默认中文；程序退出重开恢复中文
- 用户规则：没点文本框（无输入光标）时按 Shift 不切换
- 大写锁定开着 = 英文（键盘灯可监测）

## 实现思路

1. 每程序（进程/窗口）维护一张状态表：中文/英文；首次见到默认中文，进程重启归零。
2. 识别"单次轻按 Shift"：需补收键起事件——Shift 按下后 300ms 内松开、中间无其它键 = 切换一次。
3. Shift 轻按时判断焦点元素是否可输入（UIA，仅 Shift 时查，代价小）；不可输入则不翻转（防游戏误判）。
4. 大写锁定 = 英文；关闭后回到记忆状态。
5. 自愈：中文上屏 → 强制中文；英文直接上屏 → 英文；Windows+空格改布局可检测。

## 边界与局限

- 点输入法图标/Ctrl+Shift 等切换方式程序看不到，只能靠自愈纠偏。
- 微软五笔是否同规则需实测。
- 该方案是"记账"而非"偷看"：输入法内部状态无公开接口，只能事件推导。

## 2026-08-03 实测结论（方向调整）

### 直接读状态的三条路，实测结果
- WM_IME_CONTROL / IMC_GETCONVERSIONMODE：经典 IMM 程序（WPS 32 位）可读；
  但 Chromium 系（Codex/Edge/B站/终端）返回固定值 conv=0x1，用户真实切换时不变，不可用。
- TSF 语言栏跨进程枚举（ITfLangBarMgr / GetThreadLangBarItemMgr）：所有程序一律 E_FAIL，
  官方文档也标注 "should not be used"，不可用。
- 任务栏"托盘输入指示器"像素：真实反映多多五笔中英状态，但只能看出"变了"，
  看不出"变成中还是英"；悬停高亮、时钟刷新也会触发像素变化（diff 150~500），
  真切换 diff 1600~1900。旧版"见变化就翻转记忆"会乱翻（实测一轮手动切换记忆变成 True/True/False）。

### 结论
- 删除像素翻转通道（已实施）；保留 UIA 文字通道（微软五笔/拼音的"中文模式/英语模式"名称变化有效）。
- 多多五笔在 Chromium 里的中英状态没有可用的公开读取接口，维持：
  按程序记忆 + Shift/大写锁定/Win+空格 事件跟踪 + 中文上屏/英文直通自愈。
- 大写锁定切换不会改变托盘指示器（实测多次按 CapsLock 像素不变），只能靠键盘钩子跟踪。
- 若后续要根治 Chromium+多多五笔的鼠标点击切换，候选方向：Win11 新式输入法候选窗
  （系统组件）或改为带对外接口的输入法（小狼毫等）；截图/OCR/模板比对因主题与 DPI 变化不可靠，放弃。
