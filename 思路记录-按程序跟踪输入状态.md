# 思路记录：按程序跟踪输入法状态（2026-08-02）

## 背景问题

归零归零目前靠"Shift 3 秒猜测"判断中/英文模式，猜测错误时会把中文组字字母误读成英文。
根因：Chromium 类软件中读不到输入法（多多五笔/微软五笔）内部的中英开关（imc=False）。

## 用户提出的方案（已验证规则成立）

实测确认（多多五笔）：
- 单次轻按 Shift = 切换一次中/英文（第 1 次按 → 英文；第 2 次按 → 中文）
- 每个程序默认中文；程序退出重开恢复中文
- 用户规则：没点文本框（无输入光标）时按 Shift 不切换
- 大写锁定开着 = 英文（键盘灯可监测）

## 实现思路

1. 每程序（进程/窗口）维护一张状态表：中文/英文；首次见到默认中文，进程重启归零。
2. 识别"单次轻按 Shift"：需补收键起事件——Shift 按下后 300ms 内松开、中间无其它键 = 切换一次。
3. Shift 轻按时判断焦点元素是否可输入（UIA，仅 Shift 时查，代价小）；不可输入则不翻转（防游戏误判）。
4. 大写锁定 = 英文；关闭后回到记忆状态。
5. 自愈：中文上屏 → 强制中文；英文直接上屏 → 英文；Windows+空格改布局可检测。

## 边界与局限

- 点输入法图标/Ctrl+Shift 等切换方式程序看不到，只能靠自愈纠偏。
- 微软五笔是否同规则需实测。
- 该方案是"记账"而非"偷看"：输入法内部状态无公开接口，只能事件推导。

## 2026-08-03 实测结论（方向调整）

### 直接读状态的三条路，实测结果
- WM_IME_CONTROL / IMC_GETCONVERSIONMODE：经典 IMM 程序（WPS 32 位）可读；
  但 Chromium 系（Codex/Edge/B站/终端）返回固定值 conv=0x1，用户真实切换时不变，不可用。
- TSF 语言栏跨进程枚举（ITfLangBarMgr / GetThreadLangBarItemMgr）：所有程序一律 E_FAIL，
  官方文档也标注 "should not be used"，不可用。
- 任务栏"托盘输入指示器"像素：真实反映多多五笔中英状态，但只能看出"变了"，
  看不出"变成中还是英"；悬停高亮、时钟刷新也会触发像素变化（diff 150~500），
  真切换 diff 1600~1900。旧版"见变化就翻转记忆"会乱翻（实测一轮手动切换记忆变成 True/True/False）。

### 结论
- 删除像素翻转通道（已实施）；保留 UIA 文字通道（微软五笔/拼音的"中文模式/英语模式"名称变化有效）。
- 多多五笔在 Chromium 里的中英状态没有可用的公开读取接口，维持：
  按程序记忆 + Shift/大写锁定/Win+空格 事件跟踪 + 中文上屏/英文直通自愈。
- 大写锁定切换不会改变托盘指示器（实测多次按 CapsLock 像素不变），只能靠键盘钩子跟踪。
- 若后续要根治 Chromium+多多五笔的鼠标点击切换，候选方向：Win11 新式输入法候选窗
  （系统组件）或改为带对外接口的输入法（小狼毫等）；截图/OCR/模板比对因主题与 DPI 变化不可靠，放弃。

## 2026-08-03 重大进展：多多五笔托盘图标已可提取并识别（推翻"模板比对不可靠"）

### 关键事实
- 多多五笔是 IMM32 输入法：`多多五笔_x64\duoIME.ime`（64 位）、`C:\Program Files\多多五笔\x86\duoIME.ime`。
- 用 `ExtractIconEx` 已从 duoIME.ime 提取全部 24 个 32×32 图标，存放在 `C:\tmp\dd_icons_x64\`。
- 用户描述的"橙红色圆圈带斜杠（禁止图标）"就是 **icon_23**；**icon_5** 是"中"字形图标。
- 实拍对照（托盘指示器按钮 40×60，C:\tmp\traytpl_001512\c1/c2）：
  - c1（中字形）↔ icon_5 匹配度 0.805；
  - c2（橙红禁圈）↔ icon_23 匹配度 0.849。
  说明系统托盘显示的正是 duoIME.ime 里的图标资源本身，模板比对完全可行，且远强于 OCR/颜色猜测。

### 可行性验证
- 探针 `C:\tmp\TrayStateProbe.exe`（源码 C:\tmp\TrayStateProbe.cs）：
  UIA 从桌面根找"托盘输入指示器"紧凑按钮（排除含"要切换输入法"的展开按钮）→ 截取 40×60 →
  与 icon_5/icon_23 按 13~25px 多尺度滑动匹配 → 输出"中文(中)"或"英文/禁用(橙圈)"。
- 实测（当前活动输入法=微软五笔）：正确判为"未知(可能非多多五笔)"（icon5=0.507、icon23=0.019），
  证明不会误报；待多多五笔为活动输入法时验证 中文/英文 两个状态的阈值。
- 注意：托盘按钮位于副屏 2269,1380；UIA 搜索需从桌面根（Shell_TrayWnd 子树上找不到）。

### 联网调研结论
- 微软官方确认：无公开 TSF/Win32 API 可全局读取第三方输入法的中/英状态（learn.microsoft.com 2026-04 Q&A）。
- ImTip（aardio 开源）官方实现源码 = `WM_IME_CONTROL` IMC_GETOPENSTATUS/IMC_GETCONVERSIONMODE + HKL，
  与之前实测一致，官方文档也承认第三方输入法"时对时错"；该路在 Chromium 上已确认失效。
- InputTip（AHK 开源）核心 ime.ahk 同样是 IMM 消息 + 200ms 去抖/学习策略，无托盘读取。
- RedDot（Autumn-one）用 MPress 打包无法直接看实现；其 issue #4/#18 显示 VS Code/Electron 上同样失灵，
  说明它也没有解决 Chromium 的读取问题。
- 结论：对 Chromium + 多多五笔，"提取 IME 自带图标 + 托盘截图模板匹配"是目前唯一可行且已验证的旁路。

### 后续集成方案（待用户确认后实施）
1. 程序启动时（或首次遇到多多五笔时）从 duoIME.ime 提取 icon_5/icon_23 作为模板缓存。
2. 低频轮询（如 1~2 秒）定位并截取托盘指示器；发现图标变化时连续复核 2~3 次再更新状态记忆。
3. 鼠标悬停在指示器上时跳过（悬停高亮会干扰）；指示器隐藏/不可见时退回现有逻辑。
4. 局限：仅当多多五笔为活动输入法时托盘才是其图标；主题/DPI 变化时按形状匹配（阈值 0.62/0.85 实测），
   必要时重新采样校准。
