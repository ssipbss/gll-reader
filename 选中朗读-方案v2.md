# 选中朗读按钮 v2 方案（2026-08-03）

## 一、现状问题（来自实测与代码梳理）

1. **按钮位置随机**：现在定位用的是"鼠标抬起位置/当前光标 +14,+14"，不是选区位置，
   所以感觉按钮"跟着鼠标跑"、位置飘忽。
2. **幽灵出现**：显示条件太宽松——"1.5 秒内有鼠标/键盘活动 + UIA 说有选区（或拖动手势）
   就显示"。光标的零宽"选区"、点击文字、鼠标晃过、焦点变化都可能误判为选中。
3. **消失不及时/又冒出来**：隐藏逻辑只在"活动窗口内"生效，选区取消后若没有新活动，
   按钮会残留；鼠标一晃触发活动条件又可能重新显示。
4. **Office 会破坏选区**：点击按钮可能让文档失去焦点/选区，Ctrl+C 复制时把选区搞坏。
5. **WPS 等老软件无选区事件**：只能用拖动手势兜底，目前判断太粗。

## 二、开源项目取经结论

### TextLens（Tauri + Rust，Windows/macOS 划词助手）
- **UIA 隔离**：所有 UI Automation/OLE 调用放进"辅助进程"（同 exe 以
  `--textlens-selection-helper` 参数重启自己，stdin/stdout 协议通信）。第三方 UIA 提供者
  卡死/崩溃只会杀掉辅助进程，主程序和钩子线程不受污染。这是对付"UIA 静默崩溃"的终极方案。
- **钩子线程只收原始输入**：低级键盘/鼠标钩子回调只把事件塞进队列，选区读取全部在工作线程做。
- **手势分类**：拖动（位移²≥16 且时长有效）、双击、Shift+点击、Shift+方向键、快捷键手动。
- **稳定期+重试**：mouse-up 后等 12ms 再抓选区，失败后 24ms/72ms 重试（提供者异步发布）。
- **指纹去重**：源进程+文本+边界（半像素量化）做哈希，防止同一选区重复弹。
- **边界合理性校验**：矩形宽高 >0 且 <20 万像素，防垃圾坐标。
- **剪贴板兜底隔离**：无 UIA 选区时用 Ctrl+C，轮询剪贴板 10ms/次×60 次、连续 3 次稳定，
  原剪贴板内容"隔离保存"并在 5 分钟后强制恢复，防止污染用户剪贴板。
- **关闭事件**：mouseDown/滚动/键盘按下 → 关闭工具栏（但要避免点自己工具栏时误关）。

### AIA 划词助手（Electron + 原生 selection-hook）
- **定位锚点=选区端点**：优先用 UIA 的 startTop/startBottom/endTop/endBottom——
  同行：向前选放右端、向后选放左端；多行：向下选放末尾行下方、向上选放起始行上方。
- **方向感知**：向上选的按钮放在选区上方，向下选放下方，避免遮住文字。
- **不抢焦点**：工具栏 showInactive（无激活），alwaysOnTop；已显示则忽略新选区事件（去抖）。
- **程序过滤**：白名单/黑名单决定哪些程序启用。

### ImTip（aardio）
- 光标跟随定位（winex.caret）——WPS 这类无选区 API 时的定位兜底来源。

### 共同结论
真正可靠的方案是：**"手势触发 → 实体验证（UIA 选区或剪贴板）→ 去重 → 按选区端点定位 → 
事件驱动隐藏"**，而不是"活动窗口 + 有选区就显示"。

## 三、目标行为

1. 选中文字（拖动/双击/Shift+点击/Shift+方向键）→ 在**选区末尾附近**浮现"开始朗读"按钮。
2. 按钮锚定选区，**不跟鼠标跑**；自动避让屏幕边缘。
3. 取消选中/点击别处/滚动/切窗口/Esc → 按钮消失。
4. 朗读中按钮变"结束朗读"；读完自动消失；朗读中点击别处不打断（按钮保持）。
5. 不抢焦点、不破坏选区；剪贴板只在必要且隔离恢复；浏览器/Office/Codex/WPS/记事本都可用。

## 四、架构设计

### 1. 检测层（手势分类，钩子只记事件）
- 扩展现有 MouseHook/KeyboardHook：记录 down/move/up 坐标、按下时长、位移、双击、
  Shift 状态、滚动。回调内不做 UIA。
- 手势结束条件：
  - 拖动：editable 窗口内 down → 位移 ≥4px → up；
  - 双击：两次 down 间隔 <500ms、距离 <4px；
  - Shift+点击 / Shift+方向键释放；
  - 快捷键手动触发（可选）。

### 2. 验证层（有实据才显示）
- 首选 UIA（独立 STA 线程 + 1.2s 超时）：
  - TextPattern.GetSelection → GetText（非空、trim 后长度 ≥1）；
  - GetBoundingRectangles → 过滤合理矩形（>0 且 <200000）→ 得到
    startTop/startBottom/endTop/endBottom、方向、总 bounds；
  - 零宽（光标）不算选区。
- WPS/老应用兜底：拖动手势 + 剪贴板验证（Ctrl+C → 剪贴板序列号变化且内容长度合理 →
  立即恢复原内容，5 分钟后强制复查恢复）；验证通过才显示。
- 连续两次一致（间隔约 250ms）才显示，防止拖动过程中闪烁。
- 指纹去重：进程+文本+量化边界，同一选区不重复触发。

### 3. 定位层（锚定选区，不跟鼠标）
- 有 UIA 端点：
  - 同行 → 锚定选区末尾端点（向前选=右端，向后选=左端）；
  - 多行向下 → 末尾行下方；多行向上 → 起始行上方；
  - 偏移 8px；显示器工作区内翻转避让（下没空间翻上、右没空间翻左），边距 8px。
- 无 UIA 端点（WPS 剪贴板兜底）→ 鼠标抬起点 +14,+14（仅验证通过后）。
- 只有"新选区"才重新定位；已显示则保持原位。

### 4. 显示/隐藏状态机
- 显示：手势结束 + 验证通过 + 指纹变化。
- 隐藏（任一）：
  - UIA 可验证时：选区消失（150ms 防抖）；
  - Esc；
  - 滚动 / 窗口焦点离开当前窗口；
  - 点击按钮以外区域且确认无选区；
  - 朗读结束自动隐藏。
- 不因鼠标移动/悬停/纯焦点事件显示或隐藏。
- 朗读中：按钮保持"结束朗读"；选区取消不隐藏，读完才消失（Esc 可立即停）。

### 5. 朗读取词
- 优先 UIA GetText（隔离线程）；失败/不支持 → Ctrl+C 剪贴板（隔离+恢复）。
- Office：若点击按钮仍破坏选区，改用 Word UIA/COM 只读 Selection.Text，不走剪贴板。
- 保持 WS_EX_NOACTIVATE + ShowWithoutActivation（现状已有）。

### 6. 防崩溃（对应本次静默崩溃教训）
- 第一批：所有 UIA 查询移出 UI 线程（独立 STA 工作线程 + 超时），UI 线程永不等待 UIA。
- 第二批（终极）：同 exe 以 `--selection-helper` 启动辅助进程隔离 UIA（参考 TextLens），
  辅助进程卡死/崩溃自动重启，主程序零影响。

## 五、实施批次

- **第一批（低风险，先做）**：
  1. UpdateSelectionButton 重写：删除"活动+hasSel 即显示"，改为"手势结束+验证通过+两次确认"；
  2. TextReader 增加 GetSelectionInfo（文本+端点矩形+方向），定位改用选区端点；
  3. 隐藏逻辑改为"选区消失即隐藏"（保留 150ms 防抖），去掉对活动窗口的依赖；
  4. 自测矩阵：浏览器/Codex（UIA 好）、记事本、Word（UIA+剪贴板）、WPS（拖动+剪贴板）。
- **第二批**：鼠标钩子补全手势分类（双击/Shift 点击/滚动）；WPS 剪贴板隔离恢复；
  UIA 查询移独立线程+超时。
- **第三批**：辅助进程隔离 UIA；Office COM 只读取词（如需要）。

## 六、验收标准

- 选中文字 10 次，按钮 10 次出现在选区末尾附近，位置稳定不跟鼠标。
- 点文字（无选中）不出现按钮；晃鼠标不出现；取消选中按钮 ≤0.5s 消失。
- 点击按钮不破坏选区、不剪切文字（Word/Office 重点验证）。
- 朗读中可点"结束朗读"立即停止；读完全自动消失。
- 连续多轮"选中→朗读→取消→再选中"无残留按钮、无幽灵按钮。
- 程序 30 分钟以上无崩溃、无 UI 卡顿。
