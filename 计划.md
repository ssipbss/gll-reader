# 归零归零（原“跟打朗读”） 开发计划

## 一、目标

做一款在 Windows 11 上稳定运行的“一边打字、一边朗读”软件：

- 按下哪个键，就读哪个音（字母、数字、符号、功能键）；
- 中文输入法（拼音/五笔）组字上屏后，立即朗读中文；
- 快速打字时，立即打断上一句、改读最新按键，保证跟手不卡顿；
- 全局生效：浏览器、记事本、Office、WPS、微信等所有普通窗口都能用。

## 二、参考软件分析

- 示例软件：读霸（2005 年出品，32 位）；
- 问题：年代久远、32 位、钩子兼容性差，Win11 下大量软件不出声，只能靠兼容模式+老版输入法硬撑；
- 结论：不复用其代码，用现代方案重写，功能对齐甚至更好。

## 三、技术方案

| 项目 | 方案 | 说明 |
| --- | --- | --- |
| 语言/界面 | C# + WinForms | Windows 11 自带 .NET Framework 4.8，无需安装任何环境 |
| 按键捕获 | 全局低级键盘钩子（WH_KEYBOARD_LL） | 系统级监听，64 位、浏览器、Office/WPS 均可 |
| 字母/符号解析 | ToUnicodeEx | 把按键还原成实际字符（考虑 Shift、大小写、键盘布局） |
| 中文识别 | 输入法 IME 接口（Imm32） | 读取组字结果，拼音/五笔上屏后立即朗读中文 |
| 语音 | 系统自带 SAPI5 语音 | 已检测到中文“Microsoft Huihui”+英文“Zira”，零安装 |
| 流畅性 | 异步朗读+强制打断 | 每次按键立刻停掉上一句、开读新字，跟得上快速打字 |
| 权限 | 普通权限即可 | 普通窗口全部可用；管理员窗口需以管理员运行本软件（Windows 安全限制） |

## 四、功能清单

1. 字母 A-Z 朗读（英文语音）；
2. 数字 0-9 朗读（一、二、三……）；
3. 中文朗读：拼音/五笔组字完成后立即读中文；
4. 功能键朗读：回车、退格、删除、Tab、Esc、空格、方向键、F1-F12、Ctrl/Shift/Alt/Win 等；
5. 标点符号朗读：逗号、句号、感叹号、问号等；
6. 设置项：语速、音量、中英文语音选择、各类朗读开关；
7. 最小化到托盘，不打扰工作；
8. 设置自动保存。

## 五、界面设计

- 主窗口：开始/暂停按钮、状态显示、试听按钮、语音选择下拉框、语速/音量滑杆、朗读选项勾选框；
- 托盘图标：显示主界面 / 开始监听 / 暂停监听 / 退出；
- 关闭窗口时最小化到托盘。

## 六、开发步骤

1. 编写开发计划（本文件）；
2. 搭建工程骨架；
3. 实现键盘钩子 + 字符翻译；
4. 实现语音引擎封装（即时打断）；
5. 实现输入法中文读取；
6. 实现主界面与设置保存；
7. 编译 + 自动模拟按键测试；
8. 交付：exe + 使用说明。

## 七、测试方案

- 自动模拟输入字母/数字/符号/功能键，检查朗读内容是否正确；
- 实际中文输入法联调；
- 快速连续输入，确认语音跟手。

## 八、已知限制

- 若目标软件以“管理员身份”运行，本软件也必须以管理员身份运行（Windows 安全机制）；
- 系统自带语音有几十毫秒合成延迟，属正常水平；
- 目前使用系统自带音色，以后可扩展更自然的网络音色（如微软晓晓/云希）。

## 九、开发进度

- [x] 编写开发计划（本文件）
- [x] 搭建工程骨架（C# WinForms，.NET Framework 4.8）
- [x] 实现键盘钩子 + 字符翻译（WH_KEYBOARD_LL + ToUnicodeEx）
- [x] 实现语音引擎（SAPI 底层组件，中文慧慧 + 英文 Zira，即时打断）
- [x] 实现输入法中文读取（Imm32 组字结果轮询）
- [x] 实现主界面与设置保存（语速/音量/语音选择/朗读开关/托盘）
- [x] 编译通过 + 自动按键自测通过（字母/数字/标点/功能键朗读内容全部正确）
- [x] 真机联调：中文输入法组字静音、上屏朗读（已在真实桌面用微软拼音验证通过）